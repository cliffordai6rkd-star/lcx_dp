ros2_camera:
  image_shape: [480, 640]
  fps: 30
  # ROS2 topic names for different data streams
  image_topic: "/left/color/video"

  # Data capabilities
  contain_depth: false
  contain_imu: false

