# ["cubic", "quintic", "trapezoidal"]

cart_polynomial:
  interpolation_type: "quintic"
  dt: 0.002
  max_velocity: 0.06
  enable_motion: false 
  enable_online_planning: true

buffer:
  size: 6
  dim: 7

duo_buffer:
  size: 8
  dim: 14
# 是，因为这条轨迹链路里有个明显的实现问题，导致你看到的是“越小越快”的假象，不是 max_velocity 真的
#   反向生效。

#   问题根因

#   - trajectory_base.py:46 里 max_velocity 只用来算 end_time：
#       - 速度越小
#       - end_time 越大
#       - 轨迹点数量就越多
#   - 但 cartesian_trajectory.py:63 这段生成轨迹点时，没有按 dt 睡眠，而是几乎瞬间把整条轨迹塞进
#     buffer：
#       - cur_time += self.dt
#       - self._buffer.push_data(...)
#       - 没有 time.sleep(self.dt)

#   同时：

#   - hardware/base/utils.py:86 的 Buffer 是 FIFO，而且满了就丢最老的数据
#   - 你当前 cartesian_polynomial_traj_cfg.yaml:10 buffer.size = 6
#   - 控制线程 motion_factory.py:141 才按固定频率去 pop

#   这会导致什么

#   当你把 max_velocity 调小：

#   1. end_time 变长
#   2. 轨迹点变多
#   3. 轨迹线程却不是“慢慢生成慢慢执行”，而是“瞬间生成很多点”
#   4. buffer 只有 6 个位置，前面的大量慢轨迹点都被挤掉
#   5. 控制线程拿到的是靠后的点，不是从起点平滑跟过去

#   结果主观上就会变成：

#   - 机械臂没有按你想象“慢慢走”
#   - 反而像是更快地扑向后面的目标点

#   一句话总结
#   max_velocity 变小确实让规划时间更长了，但因为轨迹生产线程不按实时节奏输出，而是瞬间灌满小
#   buffer，慢轨迹前段被丢掉了，所以你看到“越小越快”。

#   你现在看到的是实现 bug，不是参数物理意义错了。

#   如果你后面要修，这条链路优先看两处：

#   - cartesian_trajectory.py:63 生成轨迹点的循环节奏
#   - cartesian_polynomial_traj_cfg.yaml:10 buffer.size 太小

#   所以当前结论很明确：

#   - 不是 max_velocity 越小真的越快
#   - 是轨迹线程和 buffer 机制让慢轨迹前段被覆盖了